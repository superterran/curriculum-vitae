{
  "name": "cv",
  "version": "1.0.0",
  "description": "Multi-format resume builder with variants support",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist && mkdir -p dist",
    "build": "npm run clean && node scripts/build.js",
    "build:variant": "node scripts/build-variant.js",
    "build:cover": "node scripts/build-cover.js",
    "build:all": "npm run build && npm run build:formats",
    "build:formats": "npm run build:md && npm run build:html && npm run build:pdf && npm run build:docx",
    "build:watch": "node scripts/build.js && node scripts/generate-markdown.js && resumed render resume.json --theme ${npm_package_config_theme} --output dist/resume.html && npm run build:docx && node scripts/add-download-links.js && cp dist/resume.html dist/index.html",
    "build:md": "node scripts/generate-markdown.js",
    "build:html": "resumed render resume.json --theme ${npm_package_config_theme} --output dist/resume.html && node scripts/add-download-links.js && cp dist/resume.html dist/index.html",
    "build:pdf": "pandoc dist/resume.md -o dist/resume.pdf --pdf-engine=xelatex 2>/dev/null || echo 'PDF generation requires LaTeX (xelatex). Skipping...'",
    "build:docx": "pandoc dist/resume.md -o dist/resume.docx",
    "build:docs": "npm run build:md && npm run build:pdf && npm run build:docx && echo '\n✅ Generated PDF and DOCX in dist/'",
    "validate": "resumed validate resume.json",
    "dev": "npm run build && concurrently --kill-others \"nodemon --watch resume --ext yaml,yml,json --exec npm run build:watch\" \"live-server dist --port=3000 --entry-file=resume.html --no-browser\"",
    "serve": "npx serve dist -p 3000",
    "release": "node scripts/release.js"
  },
  "config": {
    "theme": "jsonresume-theme-elegant"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "js-yaml": "^4.1.0",
    "serve": "^14.2.5"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "gh-pages": "^6.1.1",
    "jsonresume-theme-elegant": "^1.16.1",    "live-server": "^1.2.2",    "nodemon": "^3.0.2",
    "resumed": "^3.0.0"
  }
}
